import { StyleSheet } from 'react-native'

// export const baseValue = Math.min(vh('2%'), 16) // rem analog

export const globalStyles = StyleSheet.create({
  container: {
    paddingHorizontal: 16,
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
})
